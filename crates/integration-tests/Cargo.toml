[package]
name = "integration-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
core-tests = { path = "../core-tests" }
semantic-tests = { path = "../semantic-tests" }
syntax-tests = { path = "../syntax-tests" }
edge-cases = { path = "../edge-cases" }
fuzz-tests = { path = "../fuzz-tests" }
performance-tests = { path = "../performance-tests" }

[features]
default = ["integration"]
integration = []
