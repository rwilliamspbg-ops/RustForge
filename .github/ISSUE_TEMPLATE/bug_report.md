---
name: Bug report
about: Something in the template doesn't work as documented
title: ""
labels: bug
---

**Which crate(s)?**
e.g. `core-tests`, `fuzz-tests`, the root workspace config, CI...

**What happened**
A clear description of the bug.

**Expected behavior**
What you expected instead.

**Repro steps**

```bash
# commands you ran, e.g.
cargo test -p syntax-tests --features compile-fail
```

**Environment**
- Rust toolchain: `rustc --version`
- OS:
- Features enabled (if relevant):

**Additional context**
Anything else — logs, a link to a failing CI run, etc.
